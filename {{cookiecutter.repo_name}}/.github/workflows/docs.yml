name: Docs

on:
  workflow_dispatch:
  push:
    # trigger only on main branch
    branches:
      - main
    # trigger only on changes to the following files
    paths:
      - "{{cookiecutter.package_import_name}}/docs/**"
      - ".github/workflows/docs.yml"

# env for all jobs
env:
  PIP_CACHE_DIR: ~/.cache/pip
  # increase this value to manually reset cache
  CACHE_NUMBER: 0

jobs:
  docs:
    name: Deploy docs
    # better to use pinned version here
    runs-on: ubuntu-20.04
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Cache packages
        uses: actions/cache@v2
        with:
          {% raw -%}
          path: |
            ${{ env.PIP_CACHE_DIR }}
          key: ${{ runner.os }}-pkgs-${{ env.CACHE_NUMBER }}
          {%- endraw %}
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: "3.9.7"
      - name: Set up pip cache
        {% raw -%}
        run: python3 -m pip config set global.cache-dir ${{ env.PIP_CACHE_DIR }}
        {%- endraw %}
      - name: Install mkdocs
        working-directory: {{cookiecutter.package_import_name}}/docs
        run: python3 -m pip install -r requirements.txt
      - name: Deploy docs
        working-directory: {{cookiecutter.package_import_name}}/docs
        run: mkdocs gh-deploy --force
